<!DOCTYPE HTML>
<html>
<head>
<title>QQQFINANCE Registrazione</title>
<meta charset="utf-8">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
	<a href="/" class="link-dark"><p class="fs-1 fw-bold px-5 mt-5" style="letter-spacing: 3px;"> QQQFinance</a>
	<br>
	<h1 fw-bold>
	<% if @user != nil%>
		<% if @user.errors.any? %>
			<div class="alert alert-danger col-5 m-2">
				<ul class="list-group list-group-flush"> 
					<% @user.errors.full_messages.each do |message|%>
						<li class="list-group-item list-group-item-danger">
							<h5><%= message %></h5>
						</li>
					<%end%>
				</ul>
			</div>
		<%end%>
	<%end%>
	</h1>
	<%= form_with(model: @user, url: registration_path(@user)) do |form| %>
		
		<br>
		<div class="row">
			<div class="col px-5 fs-2 fw-bold"></div>
				<br>
				<div class="col">
					<%=form.label :nome%>
					<%= form.text_field :nome %>
					<br>
					<%=form.label :cognome%>
					<%= form.text_field :cognome %>
					<br>
					<%=form.label :username%>
					<%= form.text_field :username%>
					<br>
					<%=form.label :email%>
					<%= form.text_field :email %>
					<br>
					<%=form.label :password%>
					<%= form.password_field :password%>
					<%=form.label :pubblico%>
					<%= form.radio_button :pubblico,"si" %>
					<%= form.label :si %>
					<%= form.radio_button :pubblico,"no" %>
					<%= form.label :no %>

		
			
					<br>
					<br><br>
					
					<br>
					<br><br>
					<div class="text-center">
						<%= form.submit "Crea profilo",class:"btn btn-primary"%>
						<button type="Reset" class="btn btn-danger btn-lg m-2">Reset</button>
					</div>
				</div>
				<div class="col"></div>
				<div class="col">
					<div class="d-flex" style="height: 500px;">
						<div class="vr"></div>
					</div>
				</div>
				<div class="col"></div>
				<div class="col">
					<br><br><br><br><br>
					<p class="px-5 fs-2 fw-bold">oppure</p>
					<br>
					<button class="btn btn-primary btn-lg" type="button" name="Facebook">Accedi con Facebook</button>
					<br><br>
					<button class="btn btn-success btn-lg" type="button" name="Google">Accedi con Google</button>
				</div>
				<div class="col"></div>
				<div class="col"></div>

			</div>
		</div>
		<p class="fs-2 fw-bold px-5 text-center">Se hai già un account,vai al <a href="/users/sign_in">Login</a></p>
		<br>
	<% end %> 
</body>
</html>