<%= form_with(model: wallet) do |form| %>
  <% if wallet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wallet.errors.count, "error") %> prohibited this wallet from being saved:</h2>

      <ul>
        <% wallet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user, style: "display: block" %>
    <%= form.text_field :user %>
  </div>

  <div>
    <%= form.label :azione, style: "display: block" %>
    <%= form.text_field :azione %>
  </div>

  <div>
    <%= form.label :quantità, style: "display: block" %>
    <%= form.number_field :quantità %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
